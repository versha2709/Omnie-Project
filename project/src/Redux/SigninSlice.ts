import { createSlice } from "@reduxjs/toolkit";

const initialState = {

};

const Slice2 = createSlice({
  name: "Signin",
  initialState,
  reducers: {
    
  },
});

export default Slice2.reducer;
